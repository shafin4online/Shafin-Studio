export async function mergeImages(img1: string, img2: string, mode: string, options: { gap: number, opacity: number }): Promise<string> {
  // This is a stub to allow the build to pass. 
  // In a real scenario, this would use canvas to merge the images.
  return img1; 
}
